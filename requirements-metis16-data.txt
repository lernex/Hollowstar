# Metis-1.6 data runtime inputs.
#
# Every direct dependency is exact. Production installs use the generated,
# transitive, hash-locked requirements-metis16-data.lock file; never install
# this input file on login2 or Rhea.
pip==26.0.1
setuptools==81.0.0
wheel==0.46.3
datatrove[io,processing,s3]==0.9.0
s3fs==2025.3.0
huggingface-hub==0.36.0
hf-xet==1.5.2
datasets==3.6.0
# Multimodal holdout benchmarks (MMMU, MathVista) carry image columns.
# Decoding is disabled in holdouts.py, but declare Pillow anyway: a
# builder that does not expose features cannot be cast, and a missing
# decoder there fails inside iteration and kills the whole run.
Pillow>=10.0
PyYAML==6.0.3
pyarrow==23.0.1
tokenizers==0.22.2
numpy==2.2.6
requests==2.33.1
xxhash==3.6.0
zstandard==0.25.0
warcio==1.7.5
Brotli==1.2.0
